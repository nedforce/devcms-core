#images_bar
  - images.each do |image|
    = link_to_node(image_tag(thumbnail_image_path(image, :format => :jpg), :alt => h(image.alt)), image.node, { :action => :full, :format => :jpg}, { :rel => "lightbox[#{rel}]", :title => "#{h image.title} - #{h image.description}" })