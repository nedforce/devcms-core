= form.hidden_field :title
= form.hidden_field :description

- if current_user.has_role?('editor')
  = form.hidden_field :editor_comment

= content_box_hidden_fields form
= category_hidden_fields    form
