= javascript_tag("new Ajax.Autocompleter('category_auto', 'autocomplete', '#{ajax_admin_attachments_path}', {frequency: 0.5, paramName: 'query'})")

.right_panel_content
  %h1== #{t 'attachments.edit'} '#{h @attachment.title}'

  - admin_remote_form_for @attachment, :url => admin_attachment_path(@attachment, :format => 'html'), :update => 'right_panel_body' do |f|
    = error_messages_for :attachment

    = f.text_field :title,    :label => t('shared.title')
    = f.text_field :filename, :label => t('attachments.filename')
    = f.text_field :category, :label => t('attachments.attachment_category'), :id => 'category_auto'
    
    #autocomplete

    = f.submit t('shared.save')

    = approval_field
