= javascript_tag "treePanel.refreshNodesOf(#{@parent_node.id})"

#rightPanelDefault.rightPanelDefault
  %table
    %tr
      %td
        %p= t 'html_pages.saved'
        %p= link_to_function(t('html_pages.add_images'), "rightPanel.load({url: '#{new_admin_image_url}', method: 'GET', params: {parent_node_id: #{@html_page.node.id}}})")
