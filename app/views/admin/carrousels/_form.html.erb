carrouselDisplayTimePanel = new Ext.Panel({
  html: '<%= escape_javascript(display_time_select_for(@carrousel)) -%>',
  frame: false,
  border: false
});

carrouselTransitionTimePanel = new Ext.Panel({
  html: '<%= escape_javascript(transition_time_select_for(@carrousel)) -%>',
  frame: false,
  border: false
});

carrouselAnimationPanel = new Ext.Panel({
  html: '<%= escape_javascript(animation_select_for(@carrousel)) -%>',
  frame: false,
  border: false
});

carrouselFormPanel = new Ext.Panel({
  html: '<%= escape_javascript(content_box_settings_for(@carrousel)) %>',
  frame: false,
  border: false
});

carrouselForm.add(carrouselDisplayTimePanel);
carrouselForm.add(carrouselTransitionTimePanel);
carrouselForm.add(carrouselAnimationPanel);
carrouselForm.add(carrouselFormPanel);
