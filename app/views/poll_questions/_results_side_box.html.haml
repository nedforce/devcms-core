.poll_results
  - question.poll_options.each do |o|
    %div
      .text
        = h o.text
        %span.smaller_font== (#{number_to_percentage(o.percentage_of_votes, :precision => 1)})
      .bar
        = image_tag 'poll_result_sidebox.gif', :alt => 'Resultaat percentage', :width => number_to_percentage(o.percentage_of_votes, :precision => 0)

  %p.number_of_votes
    == #{t 'polls.number_of_votes'}: #{question.number_of_votes}
