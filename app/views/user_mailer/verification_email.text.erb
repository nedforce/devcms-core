Beste <%= @user.login -%>,

U ontvangt deze e-mail omdat u zich hebt aangemeld als gebruiker van <%= @host %>.
Alvorens u gebruik kunt maken van de functionaliteit op <%= @host %>, vragen wij u dit e-mailadres te verifiëren door het volgende adres te openen in uw browser:

<%= verification_user_url(@user,
  code: @user.verification_code,
  only_path: false,
  protocol: (DevcmsCore.config.ssl_enabled ? 'https://' : 'http://'),
  host: @host
) %>

Met vriendelijke groet,

Het <%= @host %> team
