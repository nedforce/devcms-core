Op <%= Settler[:host] -%> zijn de volgende velden ingevuld in het <%= @subject -%>:

<% @entered_fields.each do |field| %>
  <%= field[:label] -%>:
  <%= field[:value].is_a?(Tempfile) ? field[:value].original_filename : field[:value] -%>
<% end %>
